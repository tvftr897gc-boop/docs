Adds content to the existing `panel/installing-and-pregenerating-worlds-with-chunky.mdx` page covering:

- Installing Chunky via the plugin manager or manually (Bukkit/Spigot/Paper/Purpur/Fabric/Forge/NeoForge)
- Step-by-step pre-generation from the console (`chunky world`, `center`, `radius`, `start`, `status`)
- **Strong guidance to cap the radius** with a recommended-radius table (1k–10k blocks) and approximate disk usage so users don't fill their storage quota
- Performance tips (off-peak generation, Nether 1/8 ratio, throttling)
- Warning + Note callouts about storage limits

Page was already wired into the `Control Panel` group in `docs.json`, so no nav changes needed.